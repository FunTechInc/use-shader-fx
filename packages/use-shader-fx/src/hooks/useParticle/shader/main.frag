precision highp float;
precision highp int;

varying vec2 vUv;
varying vec3 vPosition;
uniform float uTime;

const float per  = 0.5;
const float PI   = 3.14159265359;

void main() {    
    gl_FragColor = vec4(1.0);
}